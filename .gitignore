node_modules
dist
.DS_Store
.firebase/*.cache
.claude/settings.local.json
# env ファイルは意図的にコミット対象（Firebase web API key はクライアント公開前提）
# ローカル上書き用は .env.local に記載すること
.env.local
.env.staging.local
.local-test-accounts.json
firebase-debug.log
.claude/worktrees/

# Local secrets and Firebase/Google service account keys must never be committed.
secrets/
*.service-account.json
*service-account*.json
*firebase-adminsdk*.json
*google-credentials*.json
